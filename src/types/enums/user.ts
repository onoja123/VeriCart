export enum UserType{
    USER = "User",
    ADMIN = "Admin",
}

export enum UserStatus {
    ACTIVE = 'Active',
    INACTIVE = 'Inactive',
    DELETED = 'Deleted',
}

export enum IdentityStatus {
    REJECTED = "rejected",
    APPROVED = "approved",
    PENDING = "pending"
}